#include <string>
#include <vector>

namespace day03 {
    int part1(const std::vector<std::string>& rows);
    int part2(const std::vector<std::string>& rows);

    int mul(const std::string& row, bool part1 = true);
}
